# Tradera XML uploads

Place archived Tradera auction XML exports in this folder.

- Keep the original filenames from Tradera when possible.
- Avoid adding sensitive personal information.
- Run `node scripts/tradera_xml_import.js` (or `python scripts/tradera_xml_import.py`) to import these files one time.
