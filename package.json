{
  "name": "pokestats",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix dashboard --include=dev",
    "heroku-postbuild": "npm run build --prefix dashboard",
    "build": "npm run build --prefix dashboard",
    "start": "node server.js",
    "dev": "npm run dev --prefix dashboard",
    "test": "node --test",
    "pt:import": "node scripts/pt_import_run.js",
    "sets:snapshot": "node scripts/set_value_snapshot.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.1",
    "@typescript-eslint/eslint-plugin": "^8.10.0",
    "@typescript-eslint/parser": "^8.10.0",
    "eslint": "^9.11.1",
    "eslint-plugin-import": "^2.31.0",
    "globals": "^15.9.0",
    "knip": "^5.34.4"
  },
  "engines": {
    "node": "20.x"
  }
}
